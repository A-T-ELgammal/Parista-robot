{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build URCap (Maven)",
      "type": "shell",
      "command": "mvn clean install",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
